# ??????????????

# ????????????????
colors = c("??????????????", "??????????", "??????????????", "????????????")

int_values = vector(mode = "integer", length = 5)
char_values = vector("character", 10)

index = 1:5

odds = seq(from = 1, by = 2, length.out = 10)
evens = seq(from = 2, to = 20, by = 2)
rev_evens = seq(length.out = 10, to = 2, by = -2)

seq(from = as.Date('2020/09/01'), by = 1, length.out = 7)
seq(from = Sys.Date(), by = 7, length.out = 5)

runif(5, 0, 100) # 5 ?????????? ???????????????????????? ?????????????????????????? ?? ?????????????????? ???? 0 ???? 100
rnorm(5, 10, 5) # 5 ?????????? ???????????????????????? ?????????????????????????? co ?????????????? = 10 ?? ?????? = 5

# ????????????????????????????

colors[1] # ???????????????????? ?? ??????????????!

length(colors)

colors[1:3] # ????????????????????????

nums = seq(from = 10, by = 10, length.out = 7)
index = c(1, 4, 2, 3)
nums[index]

# ????????????????????????????

lengths = c(28, 40, 45, 19, 38)

min(lengths)
max(lengths)
range(lengths)
mean(lengths)
median(lengths)
var(lengths)
sd(lengths)
sum(lengths)

lengths * 1000
sqrt(lengths)

stations = c(20, 21, 22, 12, 24)
dens = lengths/stations
dens

# ?????????? ?? ????????????????????

lengths2 = sort(lengths)

lengths2 = sort(lengths, decreasing = TRUE)
?paste
paste0(1:12)
paste(1:12)        # same
as.character(1:12) # same
